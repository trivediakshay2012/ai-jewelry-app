Updated in this patch:
- fixed TypeScript errors in lib/saveLead.ts
- added missing app/invite/[vendor].tsx screen with proper imports
- registered invite route in app/_layout.tsx
- removed internal "phase" language from vendor dashboard so the screen reads like a real vendor product page
- kept invite preview and vendor counts vendor-facing
